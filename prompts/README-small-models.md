# Recommendation for prompting small models

This directory contains a generic prompt suitable for larger models.

For small models it is recommended to add the input filenames directly into the prompt, thus the context does not overflow.

See tools/pcdp-lint/prompt.md for an example.